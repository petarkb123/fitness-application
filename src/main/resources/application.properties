spring.application.name=FitnessApplication

# Server
server.port=3000

# DB - MySQL Local Database (Development)
# For production deployment, use application-prod.properties with PostgreSQL
spring.datasource.url=jdbc:mysql://localhost:3306/fitness_main?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&connectionCollation=utf8mb4_0900_ai_ci
spring.datasource.username=root
spring.datasource.password=babati1528
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# Thymeleaf
spring.thymeleaf.cache=false
# Actuator if you like
management.endpoints.web.exposure.include=health

logging.level.org.springframework.security=INFO



# --- SESSION & COOKIE SETTINGS ---
server.servlet.session.timeout=7d
server.servlet.session.cookie.name=FITPOWERSESS
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.same-site=lax
# Turn this on only when you run behind HTTPS:
# server.servlet.session.cookie.secure=true

# --- REMEMBER-ME (cookie longevity; key is in SecurityConfig) 14 days ---
app.security.remember-me.cookie=FITPOWER_REMEMBER_ME
app.security.remember-me.validity-seconds=1209600




# --- EMAIL CONFIGURATION ---
# For LOCALHOST: Put your values directly here
# For PRODUCTION (Railway): Use environment variables instead (more secure)

# SMTP Settings - Configure based on your email provider
# Option 1: Google Workspace (if your email is hosted by Google)
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587

# Option 2: Microsoft 365 / Outlook
# spring.mail.host=smtp.office365.com
# spring.mail.port=587

# Option 3: Generic hosting (cPanel, etc.) - usually mail.yourdomain.com
# Option 4: Google Workspace with custom domain (uses Gmail SMTP)
# If your email is hosted by Google Workspace, use smtp.gmail.com even with a custom domain
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=office@franjohnsonhouse.com
spring.mail.password=vcoo juuu dpba kpox
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.enable=false

# App configuration
app.url=http://localhost:3000
app.mail.from=noreply@fitpower.com

# --- DEV UX ---
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.springframework.web.multipart=INFO

# --- WEB PUSH (VAPID) ---
# Generate with a VAPID tool and set in prod via env vars
vapid.public-key=BJH_A3Y5XvPlwgW5IeDys1fiFAFGCh_eFsYzl-KpAXzIJCqu-IvhFYq-XXXHyOevu3u_p9tcWjXCN67kY_lT30I
vapid.private-key=OHSFWRLAWNmvEfzAIxBHXkfTqp9Hb7bIcygAqmQ3Mu0
vapid.subject=mailto:office@franjohnsonhouse.com

